---
"@c15t/vue": patch
---

Auto-import `useExperiment()` and `useResolvedPresentation()` in Nuxt apps, so a page can read the assigned banner-experiment arm without importing from `c15t/vue/vue-plugin`.
