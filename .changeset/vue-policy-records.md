---
'@c15t/vue': major
---

Use canonical policy records and shared presentation in Vue and Nuxt. Add notice dismissal, persistent preferences, guarded drafts, distinct choice and permission callbacks, and request-time SSR record hydration. Configure layout through `presentation`; `useConsentInit()` now exposes display data; use `usePolicyRule()` for policy state. `useConsent()` exposes read-only effective permissions, and `useStoredConsent()` now returns the read-only explicit receipt instead of the writable legacy consent projection.
