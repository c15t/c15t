---
'@c15t/react': patch
'@c15t/scripts': patch
---

Keep React preferences open during pending or failed saves and close them after successful confirmation. Preserve later draft edits and explicit navigation when older requests finish. Use the same draft for compatibility manager edits and saves, including configured category scopes.

Include the shared script loader guide in the scripts package documentation so bundled React, Next.js, and JavaScript guides retain their imported sections.
