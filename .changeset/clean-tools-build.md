---
'@c15t/cli': minor
---

Generate React and Next.js consent providers with the v3 framework adapters, bind DevTools to the provider kernel, and keep JavaScript projects on the legacy store without emitting an invalid global DevTools call.
