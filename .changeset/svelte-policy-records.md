---
"@c15t/svelte": major
---

Use separate choice receipts, effective permissions and prompt requirements in Svelte. Add notice dismissal, persistent privacy access, shared policy presentation, masked preference drafts and stale-policy review before saving.

Preserve raw cookie records and the request clock during server rendering and read-only hydration without duplicate initialization. Replace legacy consent callbacks with `onChoiceRecorded` and `onPermissionsChanged`, move visual settings to `presentation`, and configure local rules with `offline({ policyRules })`.
