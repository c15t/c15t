---
"@c15t/react-native": patch
---

Draw the built-in surfaces with the same tokens as the web. The accent is the web blue, `#335CFF` and `#6685FF` in dark, where the package had been filling buttons near-black, and the neutrals, corners, spacing, and type scale now match `packages/ui`: text `#1A1A1A`, muted `#666666`, border `#E5E5E5`, 12pt cards and 8pt controls, a 4/8/16/24/32 step, 16/24 body copy, and 14pt headings and button labels. Actions are outlined rather than filled, the banner carries the web hairline and `0 8px 24px rgba(0, 0, 0, 0.12)` elevation, and its actions sit on the muted footer band with a rule above them. `Reject All` and `Accept All` share a row at equal width with `Customize` full width below, so the tertiary action is no longer a text link wedged beside two buttons. A category switch draws the 32x20 web track instead of tinting the platform `Switch`, and keeps the 44pt tap target. Text parts now apply the weight their theme declares, which never reached a device while it travelled under the name `weight`. Labels follow the web list: `We value your privacy`, `Accept All`, `Reject All`, `Save Settings`, `Privacy Settings`, `Analytics`, and `Strictly Necessary`.
