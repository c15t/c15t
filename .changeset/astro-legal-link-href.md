---
'@c15t/astro': patch
---

Render inline legal links in the server-rendered banner from `href`, the field `LegalLink` actually has, instead of `url`. The links previously rendered with no destination.
