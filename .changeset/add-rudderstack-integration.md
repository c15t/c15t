---
'@c15t/scripts': minor
---

Add a built-in RudderStack integration with the v3 `rudderanalytics` queue, required write key and HTTPS data plane URL validation, optional load options and page tracking, docs, registry metadata, and live vendor probe coverage.
