---
"@c15t/nextjs": major
---

Prepare Next.js consent from raw records, a resolved policy, and one request clock. Replace `initialConsents` and `initialHasConsented` with `InitialConsentConfig` from the server helpers. Preserve receipt metadata during hydration, skip duplicate browser initialization after server prefetch, and render choice, notice, and persistent privacy controls through the shared React presentation contract. Resolve static manifests without rewriting policy matchers.
