---
title: Quickstart
description: Add c15t to Next.js with Inth. Choose your router or static export below.
group: frameworks
---

## Choose your setup

We recommend [Inth](https://inth.com) for policy management and consent records.
For Next.js server deployments, the guides use cached manifests: your server
reuses public policy data and resolves consent for each visitor. Static exports
initialize through Inth in the browser.

| Your deployment | Start here |
| --- | --- |
| App Router with a Next.js server | [App Router](/docs/frameworks/next/app-router) |
| Pages Router with a Next.js server | [Pages Router](/docs/frameworks/next/pages-router) |
| Either router with `output: 'export'` | [Static export](/docs/frameworks/next/static-export) |

Each guide includes installation, backend prerequisites, consent-gated scripts
and a persistent preferences link. Start with the guide for your deployment; you do not need to read
the fetching reference first.

[Self-hosting](/docs/self-host/quickstart) uses the same setup with your own
backend. [Offline mode](/docs/frameworks/next/api-reference/data-fetching#offline-configuration)
resolves bundled policy and keeps choices in browser storage, with no backend
requests or records. Not recommended for production environments.

## Other setup choices

For a smaller hosted setup without server prefetch or local API routes, use
[client-side initialization](/docs/frameworks/next/client-side). Consent resolves
in the browser, so the initial HTML has no resolved consent prompt.

[Data fetching](/docs/frameworks/next/data-fetching) compares manifests and regular
backend initialization. [Server rendering](/docs/frameworks/next/server-side)
explains when to await consent or stream the page while it resolves. Same-origin
[rewrites](/docs/frameworks/next/optimization) are optional optimizations.

Try the [runnable Next.js example](/docs/examples) with both routers, a gated
YouTube video, PostHog, X Pixel and custom banner designs. The router guides use
the same script configuration. After setup, [verify requests and persistence](/docs/guides/verify-consent);
a visible banner alone does not establish that analytics wait for consent.
