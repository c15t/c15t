---
title: "Use SSR Status"
description: "Reference page for use ssr status."
group: reference
---

{/* This file is NOT rendered directly. Sections are imported by framework pages. */}

<section id="reference">
  ## Migration

  The old `useSSRStatus` hook is removed. Pass prepared server data to the provider
  and read current policy state with `usePolicyResolution` and `usePromptRequirement`.
  Use the adapter server helpers rather than a separate hydration-state store.
</section>
