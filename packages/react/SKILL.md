---
name: c15t-react-docs
description: Read and search the @c15t/react documentation. Use when working with @c15t/react — its setup, configuration, API, and behavior.
metadata:
  source: leadtype
---
# @c15t/react documentation

React v3 consent components, hooks, Inth setup and customization.

To work with @c15t/react, read its bundled docs — they ship with the package and are version-matched to the installed code:

- Start with `./AGENTS.md`; it links every per-topic Markdown file under `./docs/`.
- Prefer these local files over fetching anything over the network.
