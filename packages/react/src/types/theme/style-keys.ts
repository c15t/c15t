import type { ComponentSlots } from './style-types';

/**
 * All valid theme keys for the v2 system.
 * These correspond to the component slots defined in Theme.
 */
export type AllThemeKeys = keyof ComponentSlots;
