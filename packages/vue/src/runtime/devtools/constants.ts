/** Window property the DevTools tab page looks for in its parent frames. */
export const DEVTOOLS_BRIDGE_KEY = '__C15T_DEVTOOLS_BRIDGE__';

/** Route of the page Nuxt DevTools loads in the c15t tab. */
export const DEVTOOLS_PAGE_ROUTE = '/__c15t_devtools__';

/** Route of the tab icon. Nuxt DevTools only shows image icons from paths. */
export const DEVTOOLS_ICON_ROUTE = `${DEVTOOLS_PAGE_ROUTE}/icon.svg`;
