{
	"name": "@c15t/backend-conformance",
	"version": "1.0.0",
	"type": "module",
	"private": true,
	"description": "One HTTP conformance suite, run against both @c15t/backend and its Effect rewrite.",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"check-types": "tsc --noEmit",
		"lint": "bun biome lint ./src",
		"fmt": "bun biome format --write . && bun biome check --formatter-enabled=false --linter-enabled=false --write"
	},
	"devDependencies": {
		"@c15t/typescript-config": "workspace:*",
		"@types/node": "^24.10.1",
		"typescript": "7.0.2"
	}
}
