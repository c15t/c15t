[versions]
# Pinned deliberately. The wrapper distribution in gradle/wrapper must stay in
# lockstep with the Android Gradle Plugin minimum, see gradle-wrapper.properties.
agp = "9.2.1"
kotlin = "2.2.10"
kotlinxSerializationJson = "1.11.0"
androidxStartup = "1.1.1"
androidxLifecycle = "2.10.0"
androidxAnnotation = "1.10.0"
junit = "4.13.2"
# The instrumented-test artifacts ship as one train: a runner that disagrees with the
# rules and the ext-JUnit bridge fails at APK install time rather than at a version
# check, so the device side shares one version and the bridge shares the next.
androidxTest = "1.7.0"
androidxTestJunit = "1.3.0"

compileSdk = "36"
minSdk = "24"
targetSdk = "36"

[libraries]
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinxSerializationJson" }
androidx-startup = { module = "androidx.startup:startup-runtime", version.ref = "androidxStartup" }
androidx-lifecycle-process = { module = "androidx.lifecycle:lifecycle-process", version.ref = "androidxLifecycle" }
androidx-annotation = { module = "androidx.annotation:annotation", version.ref = "androidxAnnotation" }
junit = { module = "junit:junit", version.ref = "junit" }
androidx-test-runner = { module = "androidx.test:runner", version.ref = "androidxTest" }
androidx-test-core = { module = "androidx.test:core", version.ref = "androidxTest" }
androidx-test-junit = { module = "androidx.test.ext:junit", version.ref = "androidxTestJunit" }
kotlin-test = { module = "org.jetbrains.kotlin:kotlin-test", version.ref = "kotlin" }

[plugins]
android-library = { id = "com.android.library", version.ref = "agp" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
