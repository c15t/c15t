<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- c15t native core configuration. Android infers the transport from
           which keys are present, so there is no mode key here the way iOS has
           one. Absent means "not configured".

             com.c15t.PORTAL_URL      required: the backend base URL
             com.c15t.INIT_URL        init-route override for a proxy
             com.c15t.DOMAIN          domain sent on subject writes
             com.c15t.CATEGORIES      comma-separated category scope the app
                                      offers; iOS spells this the
                                      com.c15t.categories plist array. Absent
                                      means the full policy scope.
             com.c15t.FORCE_GPC       force the GPC signal on for a staged build
             com.c15t.reactnative.AUTO_BOOTSTRAP
                                      false only when the app starts the core
                                      itself before React Native initializes -->
      <meta-data
        android:name="com.c15t.PORTAL_URL"
        android:value="http://localhost:3000/api/self-host" />
      <!-- The same four categories examples/demo declares to its web provider
           as consentCategories, so one backend and one policy render the same
           four dialog rows on web and on mobile. -->
      <meta-data
        android:name="com.c15t.CATEGORIES"
        android:value="necessary, functionality, measurement, marketing" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- The demo's own scheme, so a reviewer at a shell can drive a consent journey
             with `adb shell am start -a android.intent.action.VIEW -d c15t-demo://accept`.
             JavaScript answers the verbs in src/deep-links, the same handler iOS runs when
             `xcrun simctl openurl` delivers the identical link. The activity is already
             singleTask, so a link that arrives while the app is up comes in as a new
             intent rather than a second instance. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="c15t-demo" />
        </intent-filter>
      </activity>
    </application>
</manifest>
