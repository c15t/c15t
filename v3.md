# c15t v3 — What It Is and Why

> Source-of-truth summary of the v3 re-architecture for PR reviewers, future
> migration docs, and content writing. Companion documents:
> `internals/rfcs/0001-consent-manifest.md` (manifest spec),
> `internals/rfcs/0002-provider-diet.md` (parked follow-up),
> `benchmarks/reports/gate-2026-07-05.md` (all benchmark numbers).

## The one-paragraph version

v3 is a **re-architecture of v2, not a feature release**. Same wire protocol,
same consent storage format — no site breaks, no visitor is ever re-prompted.
Under the hood: a zero-side-effect consent **kernel** shared by every
framework, a CDN-cacheable **consent manifest** that removes c15t from the
network critical path, a new **Vue/Nuxt integration** born on this
architecture, one **CSS system**, and a **cross-framework conformance suite**
that proves every adapter behaves identically. Every performance claim is
benchmarked, and the benchmarks killed as many ideas as they confirmed.

## The three guarantees

1. **Same wire protocol** — v3 clients speak `GET /init` + `POST /subjects` to
   every existing v2 backend, hosted or self-hosted.
2. **Same consent storage** — the kernel's persistence module writes the
   v2-compatible format (`c15t` cookie + localStorage). Upgrading never
   re-prompts a visitor. (This is tested, not promised: the conformance suite
   round-trips records between v2 core and every v3 adapter.)
3. **Same behavior, measurably faster** — the gate report is the release
   artifact: banner visible -19.6% to -22% vs v2 (React), banner in first HTML
   with CLS 0 on every SSR framework, and the manifest takes c15t's TTFB
   contribution from ~208ms to ~6ms at 200ms backend latency.

---

## v2 vs v3 at a glance

| | v2 | v3 |
|---|---|---|
| State | zustand store, side effects on construction (DOM probes, observers) | **kernel**: plain snapshot/subscribe, pure construction — no window writes, no network, no DOM until asked |
| Client | three baked-in client kinds (hosted/offline/custom) | pluggable **transports** (hosted / offline / manifest); custom = implement one interface |
| Side effects | entangled with the store | opt-in **modules**: persistence, script-loader, network-blocker, iframe-blocker |
| Init fetch | client → c15t backend, on the critical path | **manifest**: CDN-cached config + local resolution at the host's server/edge; direct `/init` remains the default & fallback |
| Framework adapters | per-framework re-implementations | thin adapters over one kernel; conformance-tested to behave identically |
| CSS | `@c15t/ui` precompiled monolith (all surfaces, always) | `@c15t/ui/styles/v3/*` CSS Modules — pay for what you render |
| SSR | banner mounts after hydration | banner in the first HTML, CLS = 0 (React, Next, Nuxt) |
| Packaging | packages: core, react, nextjs, iab, ui, … | same packages; v3 lives at **`/v3` subpaths** (`c15t/v3`, `@c15t/react/v3`, …) — unstable-by-path, root entries swap at v3 stable. `@c15t/config`, `@c15t/utils`, `@c15t/styles` dissolved into schema/kernel/ui |

## The kernel (`c15t/v3`)

The kernel is the consent engine every framework shares:

- `createConsentKernel(config)` — **pure construction**: no network, no DOM,
  no globals. Isomorphic-safe (Node/RSC/edge imports are clean);
  `sideEffects: false`.
- `getSnapshot() / subscribe()` — frozen snapshots, reference-equal when
  unchanged. React binds with `useSyncExternalStore`, Vue with a
  `shallowRef` + subscription, Svelte/Solid with their natives. This is why
  four frameworks pass one behavioral contract with thin adapters.
- `commands.init() / save() / identify()` — carried out by the configured
  transport; without one they're no-ops.
- **Consent-record model** (promoted from the Vue contractor's design — the
  strongest thinking on that branch): a policy-agnostic record storing only
  *explicit* user choices, with per-policy acknowledgements (policyId +
  fingerprint + timestamp). Interpretation happens at *read time* against the
  active policy — the same record travels EU → California without mutation
  (opt-in treats silence as denial, opt-out as permission). GPC honored;
  fingerprint change or expiry → re-prompt.

### Transports

| transport | what it does |
|---|---|
| `createHostedTransport` | `GET /init` + `POST /subjects` against a c15t backend — the v2 protocol, allowlisted headers only |
| `createOfflineTransport` | synthesizes init locally from policy packs — no backend at all |
| `createManifestTransport` | fetch-or-inline **manifest** → local resolution → same init shape; lazy GVL; sends decision inputs on save |

### Modules (opt-in side effects)

`persistence` (v2-compatible cookie/localStorage), `script-loader`
(consent-gated scripts), `network-blocker`, `iframe-blocker`. Adapters mount
them in effects; the kernel stays pure.

## The consent manifest (the headline)

**Problem:** every v2 page load does `client → c15t backend GET /init` before
the consent UI can resolve — a third-party, often cross-region round trip on
the critical rendering path (or worse, inside the host's SSR, adding 1:1 to
TTFB). Yet almost everything `/init` returns is per-tenant static data; only
geo, language, and GPC are per-request — and all three already sit in request
headers at the host's server/edge.

**Design (RFC 0001):** split `/init` into:

- **The manifest** — `GET /manifest`: geo-independent, CDN-cacheable
  (`s-maxage` + `stale-while-revalidate` + ETag), per-tenant document with
  **all** policy packs (each with a precomputed fingerprint), matching rules,
  translation inputs, branding, and the GVL **by reference** (fetched lazily,
  only in IAB jurisdictions).
- **Local resolution** — one shared, synchronous resolver in `@c15t/schema`,
  *extracted from the backend's own `/init`* and called by both the backend
  route and host middleware: `resolve(manifest, {country, region, language,
  gpc}) → InitOutput`. **Parity by construction** — there is exactly one
  implementation, plus a 24-cell byte-equal contract suite as regression
  insurance.
- **Audit via recompute-on-write** — manifest-mode saves carry the asserted
  decision inputs; `POST /subjects` re-derives the decision server-side and
  rejects stale/tampered fingerprints (409). Hosted-mode tokens unchanged.

**Non-breaking by design:** clients default to direct backend `/init` exactly
as today. Installing the Next route handlers (`@c15t/nextjs/v3/api`) or the
Nuxt module option upgrades them to same-origin manifest resolution. Static
sites inline the manifest at build time with a client geo microfetch;
degradation is always toward the *strictest* policy.

**Why it matters (measured):** direct-init SSR pays the consent backend's RTT
in the host page's TTFB (208ms at 200ms injected latency); manifest-SSR pays
~6ms. That's a **site-wide** TTFB/CWV/SEO claim, not just a banner claim.
Next client mode: 933ms → 713ms banner visible. Caveat we learned honestly:
under heavy CPU throttle, hydration hides network wins — manifest and
bundle-weight work **compound**; neither alone finishes the job.

## `@c15t/vue` (new package)

The first framework born on the v3 architecture, and the production pilot for
it (ships in the v2 release line — new package, v2 contract, kernel inside):

- **Nuxt module**: one line in `nuxt.config` + `<ConsentRoot />`. Injects
  cached Nitro routes for manifest mode (`/api/c15t/init` resolving locally
  from the cached manifest), registers auto-imported composables.
- **Framework-native I/O**: `useFetch` (SSR payload dedup — zero client init
  requests after hydration), `useCookie`, `useState`; a `#imports` stub-swap
  keeps a thin plain-Vue path.
- **Header hygiene**: only an allowlist (`accept-language`, geo headers,
  `sec-gpc`) is ever forwarded to the consent backend — never the host's
  cookies/authorization.
- **Manifest modes**: server (Nitro route) and full client mode (browser
  fetches the cached manifest once; **zero** init endpoint calls — the
  SPA/static tier).
- **Fastest measured host**: banner in first HTML, CLS 0, 39.5ms banner
  visible unthrottled.

## CSS: one system (`@c15t/ui/styles/v3`)

The Vue branch introduced per-component **CSS Modules** with `data-*`
state/variant conventions and CSS-custom-property tokens. An isolated
experiment (same page, byte-identical JS, only the CSS system swapped) settled
the question:

- **-67.5% CSS bytes** (10,061 → 3,268) for a banner site — because CSS
  follows code-splitting: IAB styles never ship to non-IAB sites
- Small consistent runtime wins (-3–4% banner visible, ~-13ms interaction)
- Structural wins: per-app hashed classes are immune to Tailwind/layer
  collisions (the repo's tw3/tw4 compat harnesses exist because the monolith
  is not); framework-neutral files; near-zero build pipeline

Decision: the modules live at `@c15t/ui/styles/v3/*.module.css` (merged into
the design-system package under the `/v3` convention; `@c15t/styles`
dissolved). Public theming contract = `data-*` attributes + tokens + the
shared slot contract, not class names.

**Status (2026-07-06): the migration is complete and shipped.**

- **Precompiled artifacts**: `@c15t/ui` publishes compiled per-component
  `dist/styles/v3/<name>.{css,js,cjs,d.ts}` with stable `c15t-ui-*` hashed
  class maps — consumers never compile CSS Modules (the Nuxt
  `build.transpile` hack is gone), and vanilla/CDN consumption works.
- **React v3 on the shared modules**: all `/v3` surfaces render from
  `@c15t/ui/styles/v3`; variants are `data-*` attributes matching Vue.
- **One theming contract**: `ConsentComponentSlots` in `@c15t/schema/config`
  — nested per-surface slot bags (`components.banner.card`, framework-native
  attr values) consumed identically by Vue (`config.components`) and React
  (`options.components`). Both frameworks carry an enforcement test asserting
  bound slot paths === declared keys *exactly*, so dead keys and unreachable
  elements (v2's "not everything was customizable") fail CI in both drift
  directions.
- **Tailwind contract, verified continuously** (`bun run compat:styles:build
  && bun run compat:styles:verify`): tw4 = zero config (bare utilities beat
  c15t via cascade layers); tw3 = one plugin line (`@c15t/ui/postcss-tailwind3`
  unwraps `@layer` before tw3's purge) + `!`-modifier overrides; no-Tailwind =
  fully styled from the module CSS alone.
- **Branding split**: branding renders eagerly on the banner (the CLS fix)
  from its own `branding` module — banner routes ship branding CSS, not
  dialog CSS; dialog CSS stays lazy until open.

**In-situ numbers** (gate report Addenda B/C — real Next routes, shipped
artifacts, no aliases): banner-route CSS transfer **10,370 B (v2 monolith) →
5,968 B (v3 modules, -42.4%)**; banner visible **-46.6% (A) / -19.7% (B)**;
interaction **-44.3% (A)**; **CLS 0.006** (was 0.22 pre-fix — root-caused via
`LayoutShift.sources` to lazy branding CSS; branding is now eager with a
stable viewport-bounded shell width); lazy-IAB routes ship **0 CSS upfront**
(~16.9KB gz arrives on dialog open). Cost side: the slot-contract
infrastructure added ~1.8KB gz JS to the v3 banner route.

## The conformance suite (`internals/conformance`)

One behavioral spec, every framework must pass it identically: storage
identity (incl. the v2 round-trip = the no-re-prompt guarantee as an
executable test), network discipline (exactly-one init, header allowlist),
consent semantics (fingerprint/expiry/GPC matrix), events, i18n, SSR.

Status: **React 20/20 · Vue 20/20 · Solid 20/20 · Svelte 20/20 client**
(Svelte SSR is a documented todo — a vitest dual-compile constraint, not an
adapter gap). The drivers are thin projections over kernel snapshots — the
practical proof that the kernel-shared architecture works. Spec-first rule
going forward: v3.x features extend the spec before any framework implements.

## Benchmarks & the gate

Full report: `benchmarks/reports/gate-2026-07-05.md` (7 iterations/scenario;
mobile = 4× CPU + Fast-4G; 200ms injected backend latency; single machine,
localhost, fixture backend — relative comparisons only).

| Gate | Verdict |
|---|---|
| SSR: banner in first HTML + CLS=0 | **PASS** — all v3 arms (v2 fails it by design; that contrast is the feature) |
| Manifest ≥150ms @ 200ms latency | **PASS** — Next client -220ms; SSR TTFB 208→5.9ms |
| No regressions | **PASS** — interaction 30.3ms vs v2's 52.9 unthrottled; repeat-visitor -21.6% |
| First Load JS -25% | **FAIL** — v3 is -1.6% vs v2 (lighter *while adding* transports/manifest/SSR); -25% belongs to a future slim entry |

### The numbers (medians; A = unthrottled/0ms, B = mobile 4×CPU + Fast-4G/200ms backend latency, C = desktop-real: unthrottled + 150ms backend RTT)

**How to read these — do not quote absolutes without this context:**

- `bannerVisibleMs` is measured **from navigation start** and includes the
  entire page pipeline (HTML fetch over the emulated network, JS download,
  parse/compile at 4× slower CPU, hydration). Under condition B the host
  app's own boot dominates: the same runs show `lastAppScriptEndMs`
  ≈ 925–1040ms. A "713ms banner" is mostly "a Next.js app on a simulated
  mid-tier phone," not consent overhead.
- **The c15t signal is the delta between arms** (e.g. v2 933 → v3 713 =
  c15t got 220ms cheaper), never the absolute.
- **For SSR arms, `bannerVisibleMs` overstates time-to-visible.** The probe
  is JavaScript and can't report until scripts run — but those arms have the
  banner **in the first HTML** (`bannerInFirstHtml=1`), so pixels are on
  screen at first paint, long before the probe fires. `bannerPaintMs`
  (Element Timing) now measures this directly: at condition B the Nuxt SSR
  arms paint the banner at **~248-280ms** while `bannerVisibleMs` says
  ~937-996ms — a ~690ms overstatement. Judge SSR arms by `bannerPaintMs`
  (+ `bannerInFirstHtml`); judge client arms by `bannerVisibleMs`.
- The **zero-consent baseline arms** (identical app shells, no c15t) put the
  page's own floor at **~68-70ms (A) / ~690-700ms (B)** — absolute banner
  timings are dominated by the host app, not consent.
- **Condition A's 0ms backend is a lab setting** (it isolates compute). The
  standing third condition is **C, "desktop-real"**: full CPU + 150ms
  consent-backend RTT — the most representative desktop profile and the
  clearest manifest-vs-direct demonstration (Addendum E).

**Desktop-real (condition C) — framework vs c15t, SSR arms (Addendum E,
current tree, same-session floors):**

| arm | banner paint | app-shell floor | c15t's share |
|---|---:|---:|---:|
| Next.js manifest-SSR | **24ms** | 41.2 | **~9ms compute, 0 network** |
| Nuxt ssr-manifest | **24ms** | 26.8 | **~1ms compute, 0 network** |
| Next.js direct-SSR | 180ms | 41.2 | ~162ms (≈ the RTT + ~10ms) |
| Nuxt ssr (direct) | 180ms | 26.8 | ~158ms (≈ the RTT + ~8ms) |

Manifest mode's TTFB stays 3-7ms at every injected latency (0/150/200ms) —
the backend RTT never reaches the page. Direct-init pays the RTT inside
TTFB on every page: at a real 150ms backend that is a 24ms banner vs a
180ms banner, site-wide. Under condition B the same structure holds with
floors at 608 (Next) / 558 (Nuxt) and banner paint at 400-504ms — in every
SSR arm the banner paints **before the zero-consent shell finishes
hydrating**. CLS 0 everywhere (authoritative-only rendering, Addendum D).

**The consent tax (bannerVisible − page floor) — the number to quote:**

| arm (condition B: mobile + 200ms) | banner visible | page floor | c15t cost |
|---|---:|---:|---:|
| React v2 full | 949.2 | 689.9 | **~259ms** |
| React v3 full | 740.8 | 689.9 | **~51ms** |
| Next v2 client | 933.2 | 699.7 | **~233ms** |
| Next v3 manifest-client | 713.3 | 699.7 | **~14ms** |
| Next v3 manifest-SSR | 729.0 | 699.7 | **~29ms** |
| Nuxt ssr (direct) † | 975.1 | 605.5 | ~370ms |
| Nuxt ssr-manifest † | 950.3 | 605.5 | ~345ms |
| Nuxt client (direct, SPA floor 784.2) † | 1312.9 | 784.2 | ~529ms |
| Nuxt client-manifest † | 1265.0 | 784.2 | ~481ms |

† gate-era Nuxt numbers, superseded below. The apparent ~90-190ms "drift" of
the current tree above the gate-era table was bisected (2026-07-05, same-day
replay: gate-era tree rebuilt and rerun back-to-back with the current tree):

- **Most of it was environmental.** The gate-era tree, replayed in the same
  session, runs +16-40ms above its recorded 00:35 numbers on every arm.
  Cross-session absolutes are not comparable; only same-session deltas are.
- **One real regression: client-manifest +82ms**, and it was the *bench
  app*, not the product. `66e4d805` switched the mount from a static
  `<ConsentRoot />` to `<component :is="'ConsentRoot'">` + `global:true` —
  Nuxt lazy-loads global components, so the resolution put a chunk fetch on
  the banner's critical path. It only shows on client-manifest because the
  manifest arm has no 200ms backend roundtrip to hide it (client-direct:
  +8ms). Fixed: the bench app statically mounts via a build-time alias
  (baseline builds get an empty stub). Worth keeping as an integration
  recommendation: **mount `<ConsentRoot />` statically; dynamic-by-name
  global resolution costs a roundtrip.**
- **Exonerated by checkpoint A/Bs:** the CSS migration (`3f518475` ≈
  gate-replay exactly), `sideEffects:false`, the dist-composables alias fix,
  the browser manifest transport, and the kernel pre-consent fixes.

**Current-tree Nuxt, one session, floors re-measured (condition B):**

| arm | visible | floor | tax | gate-era tree, same session |
|---|---:|---:|---:|---:|
| Nuxt ssr (direct) | 995.7 | 584.0 | **~412ms** | ~431ms |
| Nuxt ssr-manifest | 936.8 | 584.0 | **~353ms** | ~403ms |
| Nuxt client (direct) | 1316.9 | 756.1 | **~561ms** | ~580ms |
| Nuxt client-manifest | 1263.2 | 756.1 | **~507ms** | ~525ms |

The current tree beats the gate-era tree on every arm (lazy-split net win;
own primitives timing-neutral as measured before). For SSR arms the
paint-grounded number is the honest one: **banner pixels at ~248-280ms**
(`bannerPaintMs`), i.e. before the zero-consent floor page itself settles
(584ms) — the hydration-grounded SSR taxes above are probe-anchor artifacts,
not user-visible latency.

**v3 React/Next reduce the consent tax ~5-8×** on a simulated mid-tier phone
with a slow consent backend. Interaction agrees: the no-op floor is ~66ms (B);
v3 adds ~29ms vs v2's ~130ms. (Anchor caveat: the floor probe settles on
hydration, the banner probe on element visibility — close but not identical
anchors, so single-digit taxes are "≈0", not precisely zero. Unthrottled,
v3's banner is visible *before* the baseline app even settles: 49.6ms vs a
70.5ms floor.)

**The Nuxt floors exposed a real finding:** unthrottled, Nuxt's consent tax is
**~3ms** (39.5 visible vs 36.4 floor — the SSR banner is nearly free). But
under 4× CPU throttle the tax is far above React/Next, and TTFB proves it
isn't network (3.9ms in manifest mode) — it's parse/hydration weight. Two
fixes have since landed: the **lazy surface split** (`ConsentRoot` was
statically importing every surface incl. IAB; now only the banner is eager —
same-tree A/B: **-60 to -69ms**) and **own primitives replacing Reka**
(timing-neutral, **-10.2KB gz**). The drift is now resolved (see above): the
remaining hydration-grounded gap to React/Next (~353-561ms vs 14-51ms) is
real parse/hydration weight on the probe's anchor, but the paint-grounded
SSR numbers (~250-280ms to pixels, before the floor page settles) show users
aren't waiting on it. Note the floors also retire the cross-host CSS
confound: the tax is within-host (each arm against its own app's floor), so
it is fair even though hosts ship different assets.

**Banner visible (ms) — React host, v2 vs v3:**

| arm | A | B |
|---|---:|---:|
| full-ui (v2) | 61.7 | 949.2 |
| react-v3-full | **49.6 (-19.6%)** | **740.8 (-22.0%)** |
| headless (v2) | 51.0 | — |
| react-v3-headless | **38.0 (-25%)** | — |
| repeat-visitor (v2) | 62.7 | 949.5 |
| react-v3-repeat | **49.1** | **744.1 (-21.6%)** |

**Banner visible + TTFB — Next.js SSR arms (condition B):**

| arm | banner in first HTML | TTFB | visible |
|---|---|---:|---:|
| ssr (v2) | ❌ | 6.4 | 776.4 |
| nextjs-v3-ssr (direct init) | ✅ | **208.3** | 762.6 |
| nextjs-v3-manifest-ssr | ✅ | **5.9** | **729.0** |

(Unthrottled after the SSR-banner work: v3 direct 86.3ms, v3 manifest **63.0ms** visible.)
Direct-init pays the consent backend's RTT inside the host page's TTFB;
manifest mode removes it — a site-wide effect, not a banner effect.

**Next.js client arms (condition B):**

| arm | visible |
|---|---:|
| client (v2) | 933.2 |
| nextjs-v3-client | 717.0 |
| nextjs-v3-manifest-client | **713.3 (-219.9ms vs v2)** |

**Nuxt (`@c15t/vue`):**

| arm | A visible | B visible | B TTFB | first HTML |
|---|---:|---:|---:|---|
| ssr (direct init) | **39.5** | 975.1 | 205.7 | ✅ |
| ssr-manifest | — | 950.3 | **3.9** | ✅ |
| client | — | 1312.9 | — | — |
| client-manifest (zero init requests) | — | 1265.0 | — | — |

Nuxt is the fastest measured host unthrottled (39.5ms visible, ~3ms consent
tax — Vue string-SSR beats RSC/flight serialization) but heaviest under CPU
throttle (~370-610ms tax on current tree; lazy-split shipped -60-69ms, own
primitives -10.2KB gz, residual drift under investigation — see the tax
table). With per-host floors in place, taxes are within-host comparisons, so
the earlier cross-host CSS confound (Nuxt shipped CSS, React arms didn't) no
longer affects the quotable numbers.

**Reka UI vs own primitives (`@c15t/vue`, RFC 0003):**

| | Reka | own | delta |
|---|---:|---:|---:|
| primitives, tree-shaken (Dialog+Accordion+Switch+FocusScope) | 52.5KB / 16.5KB gz | 9.3KB-class (React ref: 3.4KB gz) | **~5×** |
| bench app total JS | 548.4KB / 177.3KB gz | 511.9KB / 167.1KB gz | **-36.5KB / -10.2KB gz** |
| banner visible (mobile+200ms) | — | — | ±1ms (neutral — savings live in the lazy manager chunk + eager path) |
| runtime deps | reka-ui + floating-ui tree | none added | -1 dependency tree |

**Interaction latency (accept click → UI updated), React:**

| | v2 | v3 |
|---|---:|---:|
| A | 52.9 | **30.3 (-43%)** |
| B | 94.4 | 95.2 (parity) |

**Bundle (gzip, c15t addition over baseline app):** v2 full 94,742 B ·
v3 full **93,206 B (-1.6%)** — v3 lighter while adding transports, manifest
resolution, and SSR banner. **CSS isolated:** monolith 10,061 B vs CSS Modules
**3,268 B (-67.5%)**.

Harness improvements shipped alongside: latency injection, CPU/network
throttling, CLS + banner-in-first-HTML + TTFB decomposition metrics, mitata
micro-benches in the pipeline, Nuxt bench host, arm mapping for v3↔v2
comparisons, honest per-request direct-init arms.

## Things the measurements killed (kept deliberately)

Recording the falsified hypotheses is the point of the discipline:

- **RSC-first banner**: built (`@c15t/nextjs/v3/rsc`), measured ≈ zero — the
  client cost is dominated by the provider/kernel graph, not banner markup.
  Kept as an optional pattern, not a recommendation.
- **Legacy-compat cut** (`@c15t/react/v3/compat`): 0 bundle bytes (already
  tree-shaken) — shipped anyway as a guarantee (kernel apps can't accidentally
  pull the v2 runtime), not as savings.
- **Provider "bloat"**: the eager path (14.8KB gz) contains only reachable
  features. The remaining diet requires a kernel API change — parked with a
  full spec in RFC 0002.
- **Chunk warm-on-intent**: real for human hover-to-click gaps, invisible to
  synthetic clicks. Shipped; unmeasurable in CI.

And two accidental finds worth more than several optimizations:

- **rslib strips `'use client'` from dist files**, breaking any Server
  Component that imports package client modules. Fixed with a post-build
  directive-restore step (`packages/shared/restore-client-directives.mjs`)
  — 332 files in `@c15t/react` alone.
- **Function-form Playwright init scripts silently die under tsx.** The
  bench perf-observer init script was serialized with
  `Function.prototype.toString` *after* esbuild's `keepNames` decorated it
  with `__name(...)` wrappers → `ReferenceError` in the page on every run.
  CLS, long-task and banner-paint capture had never actually worked; the
  navigation-timing probe had already hit the same wall (hence its string
  form). All page-context probes are now self-contained strings
  (`benchPerformanceObserverScript`). Two more Element Timing gotchas found
  while fixing it: entries only reach `PerformanceObserver`s (the
  `getEntriesByType('element')` fallbacks were dead code), and entries are
  only emitted for images/text-aggregating elements — the attribute on the
  banner *root* alone never fires; the harness now marks every banner
  descendant and takes the earliest entry.

## Follow-ups (tracked)

- **Mobile-throttle (condition B) deep-dive: DONE** (2026-07-06,
  same-session measurements):
  - **CLS is now exactly 0 by design (0.0057 → 0.00226 → 0.000000).**
    `LayoutShift.sources` overturned the Addendum B attribution: it was
    never "async policy actions" (the footer never moves) — the optimistic
    banner first painted with default copy, then the init response swapped
    it (header -20px) and blanked `common.securedBy` (branding tag -50px).
    Two fixes landed: (1) the init fold deep-merges same-language
    translations (wholesale replacement was a real kernel bug — v2 and the
    schema resolver both merge); (2) **authoritative-only rendering** — the
    kernel's `policyProvisional` keeps `activeUI='none'` while a placeholder
    policy awaits init, so no surface ever renders copy/actions an in-flight
    `/init` may replace. Kills mid-read copy swaps, wrong-language flash,
    flash-and-vanish banners, and consent recorded against a placeholder
    policy without a snapshot token; restores React/Vue behavioral parity.
    Init failure promotes the placeholder as the compliance fallback.
    Honest cost (Addendum D): the legacy hosted-client banner is now +90ms
    vs v2 at condition B (1053 vs 963 same-run) — the old "win" there was
    placeholder visibility. Eager init dispatch (kernel-creation-time, so
    the roundtrip overlaps hydration) already recovered ~43ms; the residual
    is bundle-weight/dispatch → RFC 0002. Manifest/SSR paths keep
    first-paint banners and lose nothing.
  - **Interaction B parity: BROKEN — v3 now wins.** CDP profiling showed
    v3's post-click page work was already cheaper than v2 (43.8 vs 55.0ms);
    the parity came from the kernel starting the save fetch inside the
    click task (contending with the banner-dismiss frame) plus init-race
    measurement noise. The kernel now defers `transport.save` one macrotask
    after the synchronous snapshot commit (v2's proven ordering).
    Same-session full arms at B: v2 106.9ms (p95 144.5) vs v3 **88.1ms
    (p95 92.5)** — -17.6% median, -36% p95. Bonus find: persistence's
    pending write could fire after `dispose()`, resurrecting a dead
    kernel's consent into fresh storage — fixed (flush-on-dispose).
  - **Remaining: banner visible B** (-19.7% vs unthrottled -46.6%) is
    throttled parse/hydrate weight of the eager provider graph — RFC 0002
    (provider diet / slim entry) is the lever.
- **Addendum C anomalies to bisect (Nuxt):** `client-manifest` banner visible
  reproduced at **+105.8ms (A)** over the documented table, and SSR
  `bannerPaintMs` (B) read ~492-520ms vs the documented ~248-280ms — while
  `client` (+5.5ms) and SSR *visible* stayed flat. Same signature as the
  previously-bisected tax drift (which was bench-app/environment, not
  product); today's Vue-side changes were a one-line import swap + type-only
  schema edits, so suspect harness/environment first. Needs the checkpoint-
  worktree bisect treatment before being believed.
- **Shared-logic drift audit (2026-07-06): RESOLVED same day — all 8.** The cookie and
  translations-merge bugs were instances of a class — framework packages
  re-implementing domain rules locally and drifting. Full sweep in
  `internals/audits/2026-07-06-shared-logic-drift.md`. Critical: geo/region/
  language/GPC header extraction diverges across all four frameworks (same
  request → different jurisdiction; React/Svelte even miss the `x-c15t-*`
  override headers) despite `@c15t/schema` geo-headers being canonical.
  High: accept-language parsing (3 local variants vs the translations
  package's q-value-aware one), backend-URL normalization (3 variants),
  InitOutput→KernelConfig folding (4 variants, field drift incl. branding
  and hasConsented). All consolidated: schema owns header/URL/policy-default
  interpretation, core owns init folding + subject bodies; framework-native
  acquisition (next/headers, useRequestHeaders, SvelteKit Headers) retained.
  x-c15t-* overrides always beat infra headers — pinned by schema test.
- **Styling backlog (deliberate deferrals):** React flattens footer+actionRoot
  onto one element where Vue nests them (papered over with `box-sizing:
  border-box`; true fix is DOM alignment) · `Frame` slot group waits on a Vue
  Frame (schema addition would break Vue's exact-equality enforcement test) ·
  tokens shape asymmetry (Vue flat `tokens` map vs React structured `theme`
  — same `--c15t-*` vars, unify if pushed further).
- Reconcile the bundle analyzer's route-CSS accounting with measured transfer
  (Addendum C: analyzer says +1.3KB CSS vs B, browser transfer says -1.7KB).
- Promotion sequence: schema + kernel → main → `c15t@2.x` → `@c15t/vue@2.x`
  → react/nextjs v3 majors → feature track (policy-driven surfaces, notice
  model) as v3.x
- **React → `styles/v3` CSS migration: DONE** (see the CSS section: precompiled
  artifacts, shared slot contract + enforcement tests, tw3 plugin, branding
  split, v3 Storybook stories, Addenda B/C numbers).
- RFC 0002 provider diet (with the slim/banner-only entry) — now also the
  main lever for the mobile-throttle follow-up above
- Svelte SSR conformance project; legacy provider removal at v3 stable
- **Two known flakes: DEFLAKED** — nextjs boundary-modules script test
  (queried `document.head` synchronously while the script mounts behind a
  dynamic import; now polls with a 5s ceiling) and the legacy IAB TC-string
  timing (remaining 1-2s persistence waits normalized to
  `waitForStoredValue`/5s). Verified: boundary 3/3 repeat runs, IAB e2e
  28/28 twice.
- **Paint-grounded first-visible capture: DONE** — no screencast needed; the
  element-timing nulls were the harness's own bug (init script dead on
  arrival + attribute on a non-emitting element; see the accidental find
  above). `bannerPaintMs` now lands for SSR *and* client arms across all
  three hosts via the shared init script. First result: Nuxt SSR banner
  pixels at ~248-280ms under condition B. Baseline-arm paint floor (for a
  fully paint-grounded tax) still open.
- **`@c15t/vue` lazy-split: DONE** — only the banner is eager; manager/IAB
  surfaces mount on demand with post-`load` prefetch + intent warming.
  Same-tree A/B at mobile+200ms: **-60 to -69ms** banner visible. (Lesson
  kept: prefetching on bare `requestIdleCallback` measured 130ms *worse*
  under CPU throttle — idle callbacks fire inside the loading window.)
- **Nuxt tax drift: RESOLVED** (bisected via same-day gate-era replay +
  checkpoint worktrees): mostly environmental (+16-40ms cross-session), plus
  one real +82ms client-manifest regression from the bench app's
  dynamic-by-name `ConsentRoot` mount (Nuxt lazy-loads `global:true`
  components → chunk fetch on the critical path; invisible on arms with a
  backend roundtrip to hide it). Bench app now mounts statically via a
  build-time alias; CSS migration, `sideEffects:false`, manifest transport
  and kernel fixes all exonerated. Same-session tax table is in the
  benchmarks section; current tree beats the gate-era tree on every arm.
  Two incidental bench-infra bugs fixed on the way: the Nuxt bench still
  queried the pre-parity `consent-actions-*` testids (bench was silently
  unrunnable on trees after `1f6dff5c`), and the perf-observer init script
  had never executed (the `__name` find above).
- **Own Vue primitives (RFC 0003): DONE** — Reka replaced by own primitives
  (ported from the audited React equivalents), `reka-ui` dependency removed.
  App JS **-36.5KB / -10.2KB gz**; banner-visible timing neutral (the
  savings live in the lazy manager chunk + eager path, not the measured
  banner path); conformance + suite green, CSS `data-*` contract unchanged
